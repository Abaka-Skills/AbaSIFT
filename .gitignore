# Vendor credentials — never commit. See doc/design.md §7.
test/*.json

**/__pycache__
*.py[cod]
*.egg-info/
build/
dist/
.pytest_cache/
.venv/
env/

# Default target of anything that writes (archiver, dumper): ./dump/<job>_<hash>/<ts>/<node>/
/dump/

# scratch: CLI report output at the repo root (`abasift run -o rep.json`)
/*.json
/out/
/tmp/
*.mp4
*.MP4
*.mov
*.MOV

_dev_tmp/
